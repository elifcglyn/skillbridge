# Tasks

## Completed

- Move Vite + React app into `frontend/`.
- Move Prisma and Supabase migration ownership into `backend/`.
- Add Express + TypeScript backend starter structure.
- Add frontend and backend environment examples.
- Add root orchestration scripts.
- Add project documentation files.

## Next

- Install dependencies inside `frontend/` and `backend/`.
- Verify frontend and backend builds.
- Add real local `.env` files from the example templates.
- Expand backend routes beyond `/health`.
- Add automated tests for API and frontend behavior.
